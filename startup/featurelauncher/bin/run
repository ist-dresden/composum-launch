#!/bin/bash
echo 'Starts the server in the forecround'

BINPATH=$( dirname ${0} )
CPM_HOME=$( cd $BINPATH/..; pwd )

source $BINPATH/readconfiguration.sh

$JAVA $CPM_JVM_OPTS -jar $CPM_JARFILE $FEATURE_CMD
