#!/bin/bash

#
# This script checks the server application status.
#

BINPATH=$( dirname ${0} )
CPM_HOME=$( cd $BINPATH/..; pwd )

source $BINPATH/readconfiguration.sh

set -x
$JAVA -jar $CPM_JARFILE status
